
  # Fix ReferenceError in App

  This is a code bundle for Fix ReferenceError in App. The original project is available at https://www.figma.com/design/FUgXuJRGVc0iyVN2YO5dMA/Fix-ReferenceError-in-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  